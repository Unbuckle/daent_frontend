@model IEnumerable<DAENT_FRONTEND.Models.artgr>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.artgruppe)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.artikelname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.aktuell)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.artgruppe)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.artikelname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.aktuell)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.artnr }) |
            @Html.ActionLink("Details", "Details", new { id=item.artnr }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.artnr })
        </td>
    </tr>
}

</table>
